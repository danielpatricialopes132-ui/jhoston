const normalizeEmp = (val) => { const v = (val || '').toUpperCase().trim(); if (v.includes('ECO')) return 'ECO STONE'; if (v.includes('REVEST')) return 'JHOSTON REVEST'; if (v.includes('JHOSTON')) return 'JHOSTON'; return v; }; const oEmp = normalizeEmp('ECO_STONE'); const actCtx = normalizeEmp('ECO_STONE'); const empFilt = normalizeEmp('ECO_STONE'); const matchesContext = (actCtx === 'TODAS' || actCtx === 'AMBAS') ? true : oEmp === actCtx; const matchesEmpresa = empFilt === 'TODAS' ? true : oEmp === empFilt; console.log(matchesContext, matchesEmpresa);
